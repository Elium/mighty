import * as chai from "chai";

const expect = chai.expect;

describe("Record", () => {
  it('TODO should match schema properties', () => {});
  it('TODO should generate getters and setters', () => {});
  it('TODO should be initialised', () => {});
});
